version: 2.1

# Define the jobs we want to run for this project
jobs:
  build-a:
    docker:
      - image: cimg/base:2023.03
    steps:
      - checkout
      - run: echo "this is the build job for module-a"
  test-a:
    docker:
      - image: cimg/base:2023.03
    steps:
      - checkout
      - run: echo "this is the test job for module-a"

# Orchestrate our job run sequence
workflows:
  build_and_test_a:
    jobs:
      - build-a
      - test-a
